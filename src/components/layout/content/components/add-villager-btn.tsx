import { PlusIcon } from "lucide-react";

export function AddVillagerBtn() {
  return (
    <button
      className="btn btn-emerald flex gap-2 items-center"
      onClick={() => {}}
    >
      <PlusIcon size={14} /> <span>Add Villager</span>
    </button>
  );
}
